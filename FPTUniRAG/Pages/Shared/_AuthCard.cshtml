@model FPTUniRAG.Pages.LoginModel

<section class="auth-card ui-card" data-slot="card" aria-labelledby="login-card-title">
    <h2 id="login-card-title" class="visually-hidden">Sign in to FPT UniRAG</h2>

    <div class="ui-card-content" data-slot="card-content">
        @await Html.PartialAsync("_AuthForm", Model)
    </div>
</section>
